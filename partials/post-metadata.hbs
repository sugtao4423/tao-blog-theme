<div class="post-metadata">
  <div class="date">{{date format=dateFormat}}</div>
  {{#if tags}}
  <div class="tags">
    <div class="tags__inner">
      {{#foreach tags}}
      <a href="{{url}}">{{name}}</a>
      {{/foreach}}
    </div>
  </div>
  {{/if}}
</div>